This is an entity linking task. Rank the candidates for the mention in a table.

Examples:
[EXAMPLES]

Follow the same reasoning style as the examples.
Use only the provided candidates.
If none match, still return the top list with null scores.

Steps:
1. Understand the mention in context (table row and header).
2. Compare it to candidate details (labels, aliases, descriptions).
3. Produce a top candidate list following the answer format.

Context:

- Table row (header + row):
[TABLE]
- Entity mention:
[ENTITY MENTION]
- Candidate entities:
[CANDIDATES]

[ANSWER_FORMAT]
